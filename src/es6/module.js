// En este archivo puedo tener logica separada de mi hilo principal (aplicacion)
const hello = () => {
    return 'hello!'
}

export default hello // exportamos el modulo, para ser llamado donde se necesite